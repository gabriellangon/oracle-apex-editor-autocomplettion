{
  "name": "oracle-apex-autocomplete",
  "version": "1.1.0",
  "private": true,
  "description": "SQL, PL/SQL and Oracle APEX API autocomplete for Oracle APEX code editors.",
  "scripts": {
    "test": "jest --coverage",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
